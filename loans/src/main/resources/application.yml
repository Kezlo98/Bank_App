server:
  port: 8090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/loans?currentSchema=kezlo
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
